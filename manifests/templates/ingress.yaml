---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: babe                  # Ingress name
  labels:
    app: babe
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "babe.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: babe               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
